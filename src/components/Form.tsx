import styled from 'styled-components';

const Form = (props: any) => {
   return (
      <form>
         <label htmlFor="xuizi">Xuizi:</label>
         <input type="text" name="nome" id="xuizi" />
      </form>
   );
};

export default Form;
